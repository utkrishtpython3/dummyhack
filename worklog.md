---
Task ID: 1
Agent: main (Z.ai Code)
Task: Engineer an operational GeoJSON catchment engine (`catchments.geojson`) + TypeScript spatial-hydrology controller (`hydrologyEngine.ts`) for hyper-local flash flood / debris-flow early warning in the Upper Alaknanda–Dhauliganga basin (Joshimath–Vishnuprayag–Helang gorge), plus an operational dashboard.

Work Log:
- Authored `src/data/catchments.geojson` — RFC 7946 FeatureCollection, 3 hydrologically contiguous micro-catchments (UK-CHM-UPPER → MID → LOWER), closed rings, [lng,lat] order. Verified: rings closed, shared border vertices confirm contiguity, planimetric areas 5.39 / 5.22 / 3.31 km² (declared contributing areas 6.42 / 5.38 / 4.06, all within 3.5–7.8 spec band). Full property schema: admin/cadastral, topographic (Kirpich Tc 25.5/30/29.8 min), geotechnical Green-Ampt (Ksat, porosity, ψ, recession), dynamic FFG dry/saturated thresholds, NDRF critical_infrastructure + evacuation points, downstream_target_ward_id.
- Authored `src/lib/hydrologyEngine.ts` — strict TS types (CatchmentFeature, CatchmentProperties, TelemetryInput, FlashFloodRiskVector, BasinCascadeResult). HydrologyEngine class with: `calculateDynamicThreshold` (Green-Ampt 1-hr infiltration ceiling + convex moisture interpolation between dry/saturated FFG), `estimatePeakDischarge` (Rational Q=0.278·C·I·A with non-linear sigmoid runoff C of soil-moisture + slope), `simulateBasinCascade` (kinematic-wave topological routing UPPER→MID→LOWER, transmission factor, cumulative surge + arrival lead time, debris-flow probability, NDRF directive), `getEvacuationPlan` (sorted by safe elevation differential), `computeAreaSqkm` (spherical shoelace), `haversineKm`. Server-only fs loader (`loadCatchments`/`getHydrologyEngine`) keeps the `.geojson` deliverable pristine.
- API routes: `src/app/api/hydrology/cascade/route.ts` (GET catalogue w/ geometry; POST cascade), `src/app/api/hydrology/ward/route.ts` (GET single-ward FFG + evacuation).
- Dashboard (`src/app/page.tsx` + `src/components/ews/{CatchmentMap,TelemetryControls,Results,WardDetailPanel}.tsx`): dark tactical UI, SVG catchment map (projected lng/lat → SVG, risk-colored, infra + shelter markers, river stem), per-ward telemetry sliders + Dry/Monsoon/Cloudburst presets (auto-run), cascade result cards, basin-wide alert banner + NDRF directive, ward hydro-physics panel with dynamic FFG threshold meter + evacuation plan. Sticky footer via `min-h-screen flex flex-col` + `mt-auto`.
- Fixed: Turbopack `.geojson` loader error (switched to server-side fs load + API-served geometry), ESLint `react-hooks/set-state-in-effect` (debounced fetch + render-side stale-data guard), slider/button dark-theme overrides in globals.css, preset auto-run UX.
- Verification: `bun run lint` clean. dev.log all 200, no errors. Agent Browser: page renders (VLM-confirmed map/cards/sliders/footer), Cloudburst preset → all wards CATACLYSMIC with Q cascading 149→252→310 m³/s, debris 98%, exceedance 4.26×/5.15×/5.89×; Dry preset → LOW. Ward card click → detail panel updates. Mobile 390px: single column, no horizontal overflow.

Stage Summary:
- Deliverable 1: `src/data/catchments.geojson` — valid, un-truncated, RFC 7946 compliant.
- Deliverable 2: `src/lib/hydrologyEngine.ts` — typed, production-ready, directly importable.
- Operational dashboard live on `/` (port 3000). All golden-path interactions browser-verified.

---
Task ID: 2
Agent: main (Z.ai Code)
Task: Add an alert audio/visual indicator (acoustic siren + flashing strobe on the top status badge) when an evacuation trigger fires, and a "Dispatch NDRF SITREP" button that opens a tactical emergency directive in OASIS CAP format (JSON + printable PDF).

Work Log:
- Added CSS strobe animations to `src/app/globals.css`: `.strobe-amber` (EXTREME/HIGH amber pulse, 1s ease), `.strobe-red` (CATACLYSMIC hard red strobe, 0.6s steps), `.alert-strobe-pulse` (siren-toggle pulse ring). All gated behind `prefers-reduced-motion: reduce`. Also added a print stylesheet (unused after switching to new-window print, but kept for completeness).
- Built `src/hooks/useAlertSiren.ts` — a self-contained Web Audio API siren (no audio asset files). Two detuned sawtooth oscillators (hi 760Hz ↔ lo 480Hz alternating every 0.9s) + sine LFO wail envelope (0.55Hz tremolo) → master gain → destination. API: `arm()` (unlocks AudioContext on first user gesture per autoplay policy), `start()/stop()` (fade in/out to avoid clicks), `toggleMute()` (persists to localStorage, immediately silences). Lazy mute-state init via useState initializer (SSR-safe) to satisfy `react-hooks/set-state-in-effect`.
- Built `src/app/api/hydrology/sitrep/route.ts` — POST endpoint that runs the cascade and returns a dispatch bundle: (a) `cap` = OASIS CAP 1.2 JSON-profile Alert document (identifier, sender, sent, status/msgType/scope, info[] with language/category/event/urgency/severity/certainty/responseTypes/audience/effective/onset/expires/senderName/headline/description/instruction/web/contact/parameter[]/area{areaDesc,polygon[lat,lng pairs per CAP §3.2.4],geocode[]}), severity/urgency/certainty mapped from basin alert level; (b) `sitrep` = NDRF Situation Report overlay (sitrep_id, issuing authority, coordinated_with, tactical_summary with vulnerable_population/livestock/shelter_capacity/coverage_pct, per_ward assessment with evacuation plans); (c) `cascade` = the driving BasinCascadeResult.
- Built `src/components/ews/SitrepModal.tsx` — Radix Dialog showing the formatted SITREP (CAP identifiers, tactical summary, per-ward table, critical assets + evacuation plan, CAP area polygon). Three actions: (1) Copy JSON (clipboard), (2) CAP-JSON download (Blob → anchor), (3) Print / Save PDF — opens a new browser window with a self-contained A4-portrait printable HTML document (white bg, monospace tables, RESTRICTED classification banner, alert box, directive block) and triggers `window.print()` for browser print-to-PDF. Derived `loading` state + reqId-guarded fetch to satisfy `react-hooks/set-state-in-effect`.
- Wired `src/app/page.tsx`: added `useAlertSiren` hook; `arm()` listener on first pointerdown/keydown; effect drives `start()/stop()` from `basin_wide_alert_level` (EXTREME/CATACLYSMIC → start, else stop, respecting mute); header now has a siren mute toggle button (Volume2/VolumeX icons, pulses with `alert-strobe-pulse` when active), the AlertBadge accepts `active` prop and applies `strobe-red`/`strobe-amber` classes only when the siren is firing; added amber "Dispatch NDRF SITREP" button (Send icon) that opens the SitrepModal.
- Fixed: `useRef` not imported in SitrepModal (caused client-side ReferenceError → 500), ESLint `react-hooks/set-state-in-effect` in both useAlertSiren (lazy useState init + ref-sync effect) and SitrepModal (derived loading + reqId-guarded async callbacks).
- Verification (Agent Browser): page loads clean (no console errors). Cloudburst preset → all wards CATACLYSMIC, header badge gets `strobe-red` class, mute-toggle button gets `alert-strobe-pulse` (siren armed). VLM confirmed strobing red badge + visible Dispatch button + red/rose risk coloring + no layout breakage. Dispatch SITREP modal opens → fetches `/api/hydrology/sitrep` (200) → renders full CAP document (identifier NRSC-FFEWS-...-CATACLYSMIC, headline "EXTREME — Flash Flood & Debris Flow warning", urgency Immediate, certainty Observed, NDRF RED directive). Dry preset → de-escalation: siren stops, strobe removed, risk LOW. Footer sticky at doc bottom confirmed. Mobile 390px: no horizontal overflow, header buttons wrap. `bun run lint` clean.

Stage Summary:
- Audio/visual indicator: Web Audio siren + CSS strobe (amber for EXTREME, red for CATACLYSMIC) on the header AlertBadge + mute toggle, fires only on active evacuation triggers, persists mute pref, respects prefers-reduced-motion.
- Dispatch SITREP: full OASIS CAP 1.2 JSON-profile alert + NDRF situation report, downloadable as CAP-JSON and printable to PDF via a generated standalone HTML document. Field-ready.
- All browser-verified end-to-end (arm → cloudburst → siren+strobe → dispatch modal → CAP content → de-escalation).
